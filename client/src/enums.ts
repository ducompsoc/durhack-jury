export enum JudgeSortField {
    Name,
    Email,
    Seen,
    BatchesSubmitted,
    Updated,
    None,
}

export enum ProjectSortField {
    Name,
    GuildLocation,
    Score,
    Seen,
    Updated,
    None,
}
