export enum JudgeSortField {
    Name,
    Email,
    KeycloakUserId,
    Seen,
    BatchesSubmitted,
    Updated,
    None,
}

export enum ProjectSortField {
    Name,
    GuildLocation,
    Score,
    Seen,
    Updated,
    None,
}
